---
title: "Exceptions"
outline: [2, 3]
---

# Exceptions

Всего вопросов: **24**


### Материалы для углубления

Материалы для тем, где полезно разобраться глубже:

- **Статья:** [Исключения в Java: checked и unchecked](https://habr.com/ru/companies/golovachcourses/articles/225585/)
- **Статья:** [Как обрабатывать исключения в Java](https://habr.com/ru/companies/otus/articles/971192/)

---

**• Средний приоритет**

## 1. Поясните, что такое Error и Exception?

Exception - это исключения выполнения программы
Error - ошибки JVM, программисту не стоит обрабатывать их через try-catch

---

**• Средний приоритет**

## 2. Поясните, что такое механизм try-with-resources?

Это механизм, который автоматические закрывает ресурс, объявленный в круглых скобках `try()`. В качестве ресурса можно использовать любой объект, класс которого реализует интерфейс `AutoClosable` или `Closable` (Scanner, к примеру)

---

**• Низкий приоритет**

## 3. Поясните, что такое сигнатура метода?

Это имя метода плюс параметры (порядок параметров имеет значение из-за множественной передачи данных через троеточие, которое должно располагаться последним). В сигнатуру метода не входит возвращаемое значение, а также бросаемые им исключения.
А сигнатура метода в сочетании с типом возвращаемого значения и бросаемыми исключениями называется контрактом метода.

---

**• Низкий приоритет**

## 4. Поясните, что такое явное и неявное приведение типов? В каких случаях в java нужно использовать явное приведение?

Неявное приведение – автоматическое расширение типа переменной от меньшего к большему.

Явное приведение -  явное сужение от большего к меньшему. Нужно явно указать сужаемый тип.

В случае с объектами мы можем делать неявное(автоматическое) приведение от наследника к родителю, но не наоборот, иначе получим ClassCastException.

---

---

**• Высокий приоритет**

## 5. Расскажите подробнее про механизм обработки исключений в java (Try-catch-finally)

Блок **try** - блок, содержащий код, который может выбросить исключение при выполнении
Блок **catch** - блок, “ловящий” выброшенные в **try** исключения при соответствии его типа с ожидаемым
Блок **finally** - блок, выполняющийся после блоков **try** и **catch** вне зависимости от того, было ли выброшено исключение. К примеру, чтобы освободить какой-то ресурс

---

**• Высокий приоритет**

## 6. Расскажите подробнее про обрабатываемые и необрабатываемые исключения

Проверяемые (checked) исключения обязаны быть явно обработаны до компиляции (Exception и некоторые наследники)
Непроверяемые (unchecked) не требуют явной обработки (Error, RuntimeException)

---

**• Высокий приоритет**

## 7. Как выглядит иерархия исключений в Java?

Исключения делятся на несколько классов, но все они имеют общего предка — класс `Throwable`, потомками которого являются классы `Exception` и `Error`.

__Ошибки (Errors)__ представляют собой более серьёзные проблемы, которые, согласно спецификации Java, не следует обрабатывать в собственной программе, поскольку они связаны с проблемами уровня JVM. К примеру, исключения такого рода возникают, если закончилась память доступная виртуальной машине.

__Исключения (Exceptions)__ являются результатом проблем в программе, которые в принципе решаемы, предсказуемы и последствия которых возможно устранить внутри программы. Например, произошло деление целого числа на ноль.

---

**• Средний приоритет**

## 8. Каким образом создать собственное («пользовательское») исключение?

Наследоваться от RuntimeException (рекомендуется, но можно и от Exception), переопределить методы при нужности

---

**• Средний приоритет**

## 9. Поясните: В каком порядке следует обрабатывать исключения в catch блоках?

От наследника к родителю (от более специфичных к общему). В противоположном случае до наследников невозможно добраться, т.к. родительское исключение будет перехватывать всё

---

**• Средний приоритет**

## 10. Поясните: О чем говорит ключевое слово throws?

Ключевое слово `throws` говорит о том, что метод может выбросить исключение указанных типов. Передаёт обработку исключения вышестоящему методу.

---

**• Средний приоритет**

## 11. Поясните: Может ли один блок catch отлавливать сразу несколько исключений?

Да

---

**• Средний приоритет**

## 12. Поясните: Ошибка OutOfMemoryError. Поймать утечку памяти и пофиксить

Ошибка `OutOfMemoryError` возникает, когда JVM не может выделить достаточно памяти для выполнения программы. Это может быть связано с утечками памяти, неправильной конфигурацией или большим объемом данных. Вот пошаговый процесс, как выявить и устранить причину:
1. Сделать дамп памяти
2. Проанализировать дамп, найти причины утечек. Типичные источники:
    - неосвобождённые ресурсы
    - переполненные коллекции
    - статические ссылки (в т.ч. статические коллекции)
    - неотписавшиеся слушатели
3. Устранить причины

---

**• Средний приоритет**

## 13. Поясните: Всегда ли выполняется блок finally? Существуют ли ситуации, когда блок finally не будет выполнен?

Да, кроме случаев завершения программы или JVM. `finally` может не выполниться, если в блоке `try` вызывается `System.exit(0)`

---

**• Низкий приоритет**

## 14. Поясните: StackOverflowError vs OutOfMemoryError

`StackOverflowError` - выбрасывается при переполнении стека (в стеке не хватает памяти). Обычно возникает при рекурсиях. Можно решить через `-Xss` параметр JVM, но как правило, проблема именно в рекурсии, её и нужно решить.

`OutOfMemoryError` - выбрасывается когда JVM не может выделить достаточно памяти в куче для создания нового объекта. Возможные причины:
- создание очень большого массива или объекта
- утечки памяти (неочищенные ссылки на объекты)
- чрезмерное создание объектов без их удаления
- чрезмерное ограничение памяти кучи через JVM параметр `-Xmx` (по умолчанию 256 МБ)
Варианты решения:
- использовать профилировщики памяти для анализа (`VisualVM`, `MAT`)
- удалять ненужные объекты (`list.clear()`, `null`-ссылки)
- увеличить ограничение памяти JVM (`-Xmx1024m`)
- оптимизировать управление памятью (использовать `WeakReference`, `SoftReference`)

---

**• Низкий приоритет**

## 15. Поясните: @SneakyThrows в Lombok

`@SneakyThrows` — это аннотация Lombok, которая даёт возможность автоматически обрабатывать исключения, не требуя явного указания `try-catch` или объявления `throws` в методе. Lombok оборачивает вызов в блок, который выбрасывает исключение как `RuntimeException`

```java
@SneakyThrows
public void riskyMethod() {
    throw new IOException("Exception example"); // Не нужно объявлять throws IOException
}
```

---

**• Низкий приоритет**

## 16. Поясните: Возможно ли использование блока try-finally (без catch)?

Да, в таком случае после `try` выполнится `finally`

---

**• Низкий приоритет**

## 17. Какие именно поля не будут сериализованы при сериализации? Будет ли сериализовано final поле?

1) Добавить к полю модификатор transient. В таком случае после восстановления его значение будет null.

2) Сделать поле static. Значения статических полей автоматически не сохраняются. 

3) Поля с модификатором final сериализуются как и обычные. За одним исключением – их невозможно десериализовать при использовании Externalizable, поскольку final-поля должны быть инициализированы в конструкторе, а после этого в readExternal изменить значение этого поля будет невозможно. Соответственно, если нужно сериализовать объект с final-полем неоходимо использовать только стандартную сериализацию.

---

**• Низкий приоритет**

## 18. Поясните: Какое исключение выбрасывается при возникновении ошибки в блоке инициализации класса?

Если возникшее исключение - наследник RuntimeException:
-для статических блоков инициализации будет выброшено java.lang.ExceptionInInitializerError;
-для нестатических будет проброшено исключение-источник.

Если возникшее исключение - наследник Error, то в обоих случаях будет выброшено java.lang.Error.

Если исключение: java.lang.ThreadDeath - смерть потока. В этом случае никакое исключение выброшено не будет.

---

**• Низкий приоритет**

## 19. Поясните: Какой оператор позволяет принудительно выбросить исключение?

Throw

---

**• Низкий приоритет**

## 20. Поясните: Могут ли классы быть статическими?

Класс можно объявить статическим за исключением классов верхнего уровня.
Такие классы известны как «вложенные статические классы» (nested static class).

---

**• Низкий приоритет**

## 21. В чём различие между checked и unchecked исключениями?

В Java все исключения делятся на два типа:

+ __checked (контролируемые/проверяемые исключения)__ должны обрабатываться блоком `catch` или описываться в заголовке метода (к примеру, `throws IOException`). Наличие такого обработчика/модификатора в заголовке метода проверяется на этапе компиляции;
+ __unchecked (неконтролируемые/непроверяемые исключения)__, к которым относятся ошибки `Error` (например, `OutOfMemoryError`), обрабатывать которые не рекомендуется и исключения времени выполнения, представленные классом `RuntimeException` и его наследниками (например, `NullPointerException`), которые могут не обрабатываться блоком `catch` и не быть описанными в заголовке метода.

---

**• Низкий приоритет**

## 22. Поясните: Что произойдет если исключение будет выброшено из блока catch после чего другое исключение будет выброшено из блока finally?

finally-секция может «перебить» throw/return при помощи другого throw/return

---

**• Низкий приоритет**

## 23. Поясните: Что произойдет если исключение будет выброшено из блока catch после чего другое исключение будет выброшено из метода close() при использовании try-with-resources?

В try-with-resources добавленна возможность хранения "подавленных" исключений, и брошенное try-блоком исключение имеет больший приоритет, чем исключения получившиеся во время закрытия.

---

**• Низкий приоритет**

## 24. Поясните: Что произойдет, если в блоке инициализации возникнет исключительная ситуация?

Для нестатических блоков инициализации, если выбрасывание исключения прописано явным образом, требуется, чтобы объявления этих исключений были перечислены в throws всех конструкторов класса. Иначе будет ошибка компиляции. 

Для статического блока выбрасывание исключения в явном виде, приводит к ошибке компиляции.

---
