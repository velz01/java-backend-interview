# Java Backend Interview

Единый VitePress-сайт. Загружайте содержимое архива в корень репозитория с заменой старых файлов.

Проверка: `python3 scripts/validate_site.py`
Сборка: `npm install && npm run docs:build`
